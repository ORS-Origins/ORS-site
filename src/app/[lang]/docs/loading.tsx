// Docs loading boundary: shows feedback while documentation routes prepare.
import { LocalizedRouteLoading } from '@/components/localized-route-loading';

export default function DocsLoading() {
  return <LocalizedRouteLoading />;
}
